
<?php 

$servername = "localhost";
$database = "google_api";
$username = "root";
$password = "";
// Create connection
$conn = new  mysqli($servername, $username, $password, $database);
// Check connection
if (!$conn) {
   die(


   $d=	": " . connect_errno());

         $json['conexion']=$d;
              
                
                // echo json_encode($json);
}

  $json['conexion']= true;
  // echo json_encode($json);


 ?>
